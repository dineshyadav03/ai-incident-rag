OLLAMA_MODEL=llama3.2:3b

# Optional: set GROQ_API_KEY to generate via Groq's free-tier hosted API
# instead of local Ollama (fast regardless of local machine load, still
# zero monetary cost). Get a free key at https://console.groq.com/keys
# GROQ_API_KEY=
# GROQ_MODEL=llama-3.1-8b-instant
